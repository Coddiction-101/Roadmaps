# 🔥 **PHASE 3 — Advanced JavaScript (Core JS Brain)**

### **1. Scope & Closures**
- Global, block, function scope
- Execution context
- Lexical scope
- Closure definition
- Closure use cases (private counter, encapsulation)

### **2. `this` Keyword**
- `this` in global scope
- `this` in functions
- `this` inside objects
- `this` inside event handlers
- Arrow functions (lexical `this`)
- `bind`, `call`, `apply`

### **3. Object-Oriented JavaScript (OOJS)**
- Constructor functions
- Prototypes & prototype chain
- `new` keyword
- ES6 classes
- `extends` & `super`
- Private fields (`#`)

### **4. Asynchronous JavaScript**
- Synchronous vs asynchronous
- Callbacks & callback hell
- Promises (`resolve`, `reject`, `then`, `catch`)
- `async`/`await`
- Promise chaining

### **5. Fetch API + HTTP**
- GET / POST basics
- `fetch` → response → JSON
- Headers
- Status codes
- `response.ok` handling
- `try`/`catch` for async
- REST basics
