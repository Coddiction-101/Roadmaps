# 🌿 **PHASE 2 — DOM, Events & Browser APIs**

### **1. DOM Basics**
- DOM tree
- Selecting elements
- Reading/updating text & attributes
- Creating/removing elements
- `classList` (`add`/`remove`/`toggle`)
- Styling elements

### **2. Events**
- `addEventListener`
- `removeEventListener`
- Events: `click`, `input`, `change`, `submit`, `keyup`, `mouseover`
- Event object (`target`, `type`)
- `preventDefault`
- Bubbling & capturing
- Event delegation

### **3. Forms & Validation**
- Reading values
- Preventing form submit
- Conditional error messages
- `pattern` attribute vs custom regex

### **4. Timers**
- `setTimeout` / `clearTimeout`
- `setInterval` / `clearInterval`
- Real use cases: delaying UI, auto-refresh

### **5. Browser Storage**
- `localStorage` API
- `sessionStorage` API
- Storing JSON
- Cookies basics
