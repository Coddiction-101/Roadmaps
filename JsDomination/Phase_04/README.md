# 🚀 **PHASE 4 — Projects, Patterns & Performance**

### **1. JavaScript Design Patterns**
- Module pattern (IIFE)
- Revealing module pattern
- Factory pattern
- Observer pattern (Pub/Sub)

### **2. Performance Optimization**
- Debouncing
- Throttling
- Lazy loading (IntersectionObserver)
- Code splitting
- Avoiding reflows/repaints
- Fixing memory leaks

### **3. Architecture & Thinking**
- Pure vs impure functions
- Functional programming basics (map/filter/reduce pipeline)
- Separation of concerns (logic vs DOM)
- Utility functions (deep clone, custom map)
- How JS works internally
    - Event loop
    - Call stack
    - Web APIs
    - Microtasks vs macrotasks

### **4. Interview Prep**
- Hoisting
- `==` vs `===`
- Closures
- Event delegation
- Prototype & inheritance
- `call`/`apply`/`bind`
- `null` vs `undefined`
- `map` vs `filter` vs `reduce`
- How `async`/`await` works
- Debounce & throttle
- Memory leaks
- Event loop phases
